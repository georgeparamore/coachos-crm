// This site uses plain CSS (no Tailwind). A local, empty PostCSS config keeps
// the build from walking up into the parent coachos repo and picking up its
// Tailwind PostCSS config.
const config = {
  plugins: {},
};

export default config;
