<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Cross-project build log

George keeps a running build log across all his projects in a separate repo:
`georgeparamore/dev-log` (add it with `add_repo` if it's not already in this
session). It has one file per project — this project's is
`projects/coachos.md`.

- If asked what's been built here, or for context from past sessions, check
  that file (and `dev-log/CLAUDE.md` for the full log format/conventions).
- After completing meaningful work in this repo, append a dated entry to
  `projects/coachos.md` in `dev-log` (don't rewrite prior entries) and push it
  directly to `dev-log`'s `main` branch.
